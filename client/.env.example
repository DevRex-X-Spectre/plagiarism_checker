# Client environment (for reference — most config is handled by Vite proxy)
# In development, API calls are proxied to http://localhost:10000
# In production, the server serves the built client statically

VITE_APP_TITLE=Project Archive
