# Client environment
# In development, keep VITE_API_BASE_URL=/api so Vite proxies requests to
# http://localhost:10000 through client/vite.config.js.
# In production, the server serves the built client and /api from the same origin.

VITE_APP_TITLE=Project Archive
VITE_API_BASE_URL=/api
